import { ExternalLink } from "lucide-react";
import type { Resource } from "../../models/cohort";
import styles from "./Resources.module.css";
export function Resources({ resources }: { resources: Resource[] }) { const groups = Array.from(new Set(resources.map((resource) => resource.group))); return <section className={styles.section}><div className={styles.header}><span>Resources</span><h2>Tools for this cohort</h2><p>Downloads, templates, slides, links, and reference material grouped by use.</p></div><div className={styles.groups}>{groups.map((group) => <div className={styles.group} key={group}><h3>{group}</h3>{resources.filter((resource) => resource.group === group).map((resource) => <article className={styles.card} key={resource.id}><div><strong>{resource.title}</strong><p>{resource.description}</p><span>{resource.kind} · {resource.meta}</span></div><button type="button" aria-label={`Open ${resource.title}`}><ExternalLink size={16} /></button></article>)}</div>)}</div></section>; }
